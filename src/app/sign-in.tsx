// Route wrapper — the implementation lives in src/screens (T057), matching
// the 5-line-wrapper convention the other routes follow.
import { SignInScreen } from '../screens/SignInScreen';

export default SignInScreen;
