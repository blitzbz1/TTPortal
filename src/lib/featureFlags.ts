/**
 * Compile-time feature flags.
 *
 * SOCIAL_AUTH_ENABLED: Google/Apple sign-in is built but not yet enabled
 * (provider setup pending). Flip to true to show the social buttons on
 * the sign-in screen again.
 */
export const SOCIAL_AUTH_ENABLED = false;
